<?xml version="1.0" encoding="utf-8"?>
<!-- Yaklaşan Etkinlik widget'ı (iOS UpcomingEventWidget paritesi, 2x2 / 4x2).
     Üst: takvim ikonu + "Yaklaşan Etkinlik" kicker + "hangel" wordmark.
     Orta: etkinlik adı. Alt: kalan süre (coral) + şehir satırı. Değerler runtime'da
     SharedPreferences("hangel_widget")'tan doldurulur. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/event_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_container_bg"
    android:padding="14dp">

    <!-- Üst başlık: ikon + kicker + wordmark -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:src="@drawable/ic_widget_calendar"
            android:contentDescription="@string/widget_upcoming_label" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:text="@string/widget_upcoming_kicker"
            android:textColor="@color/widget_text_secondary"
            android:textSize="11sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/widget_wordmark"
            android:textColor="@color/hangel_coral"
            android:textSize="13sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Orta: etkinlik adı -->
    <TextView
        android:id="@+id/event_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:maxLines="2"
        android:ellipsize="end"
        android:text="@string/widget_upcoming_empty"
        android:textColor="@color/widget_text_primary"
        android:textSize="16sp"
        android:textStyle="bold" />

    <!-- Esnek boşluk -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Kalan süre -->
    <TextView
        android:id="@+id/event_countdown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="@string/widget_upcoming_empty_sub"
        android:textColor="@color/hangel_coral"
        android:textSize="22sp"
        android:textStyle="bold" />

    <!-- Şehir (veri yoksa GONE) -->
    <TextView
        android:id="@+id/event_city"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:text=""
        android:textColor="@color/widget_text_secondary"
        android:textSize="11sp"
        android:textStyle="bold"
        android:visibility="gone" />

</LinearLayout>
