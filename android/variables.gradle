ext {
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
    // Firebase: messaging + crashlytics AYNI BoM'dan (34.13.0) eşleşmiş sürümler.
    // Pin'siz default'lar (messaging 25.0.1 + crashlytics 20.0.3) farklı BoM'lardandı →
    // tutarsız firebase-common → ComponentMonitor.processRegistrar IllegalStateException →
    // app açılır açılmaz çöküyordu (Play Vitals: 2.0.4/2.0.5 çökmelerinin %84'ü).
    firebaseMessagingVersion = '25.0.2'
    firebaseCrashlyticsVersion = '20.0.6'
}